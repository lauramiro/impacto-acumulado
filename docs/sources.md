# Data sources

All values below were observed directly from live API responses and downloaded files captured on 2026-09-18 (fixtures for BOE/BOJA cover 2022-2024 documents; the reference layers and Groq model list reflect what was live on the capture date). Fixtures live under `pipeline/tests/fixtures/`.

## BOE

- Summary endpoint: `https://www.boe.es/datosabiertos/api/boe/sumario/{yyyyMMdd}` (path date, no query params). Example: `https://www.boe.es/datosabiertos/api/boe/sumario/20230918`.
- Headers: `Accept: application/json` works as documented in the brief; no other header was needed.
- Response envelope (confirmed from `pipeline/tests/fixtures/boe_sumario_20230918.json`, 351611 bytes):
  ```
  { "status": {"code": "200", "text": "ok"},
    "data": {"sumario": {"metadatos": {...},
      "diario": [ {"numero": "223", "sumario_diario": {...},
        "seccion": [ {"codigo": "3", "nombre": "III. Otras disposiciones",
          "departamento": [ {"codigo": 9575, "nombre": "MINISTERIO PARA LA TRANSICIÓN ECOLÓGICA Y EL RETO DEMOGRÁFICO",
            "epigrafe": [ {"nombre": "Impacto ambiental",
              "item": [ {"identificador": "BOE-A-2023-19635", "control": "2023/19437", "titulo": "...", "url_pdf": {...}, "url_html": "...", "url_xml": "..."} ] } ] } ] } ] } ] } } }
  ```
- JSON keys (for the item with `identificador` = `BOE-A-2023-19635`):
  - section code = `seccion.codigo` (e.g. `"3"`), section name = `seccion.nombre` (`"III. Otras disposiciones"`)
  - department name = `departamento.nombre` (`"MINISTERIO PARA LA TRANSICIÓN ECOLÓGICA Y EL RETO DEMOGRÁFICO"`, all caps in this JSON; note the XML document below gives the same department in title case)
  - epigrafe name = `epigrafe.nombre` (`"Impacto ambiental"`) - this is the most direct category filter for environmental-impact resolutions
  - item id = `item.identificador`, title = `item.titulo`, xml url = `item.url_xml` (`https://www.boe.es/diario_boe/xml.php?id=BOE-A-2023-19635`)
- Document XML (`GET https://www.boe.es/diario_boe/xml.php?id=<identificador>`, no headers needed): root element `<documento>`.
  - identifier element: `<metadatos><identificador>`
  - title element: `<metadatos><titulo>`
  - publication date element: `<metadatos><fecha_publicacion>` (format `YYYYMMDD`); there is also `<fecha_disposicion>` (signature date of the resolution, same format)
  - department element: `<metadatos><departamento codigo="9575">Ministerio para la Transición Ecológica y el Reto Demográfico</departamento>` (title case here vs. all-caps in the summary JSON)
  - body text element: top-level `<texto>` (direct child of `<documento>`, after `<analisis>`) containing `<p class="...">` paragraphs. Caution: a second, unrelated `<texto>` element also appears nested inside `<analisis><referencias><posteriores><posterior>` (short annotation text about a later correcting BOE entry) - a parser must select the top-level `<documento>/<texto>`, not the first `<texto>` found in document order in some libraries' traversal, since XPath `//texto` would match both.
- Selection rules: filter summary items where `epigrafe.nombre` == `"Impacto ambiental"` and `departamento.nombre` matches the Ministry for the environment (name observed: `"MINISTERIO PARA LA TRANSICIÓN ECOLÓGICA Y EL RETO DEMOGRÁFICO"` / `"Ministerio para la Transición Ecológica y el Reto Demográfico"` - this name is stable across the 2022-2025 fixtures found, unlike BOJA's department name below). Within the document body, the verdict is stated near the end as `"formula declaración de impacto ambiental [desfavorable] a la realización del proyecto/de la línea ..."` - presence of the word `"desfavorable"` immediately after `"impacto ambiental"` in that operative sentence marks an unfavorable verdict; its absence there marks favorable. Some resolutions cover multiple named installations with different verdicts per installation in the same document (see BOE-A-2023-2580 below) - a parser must not assume one verdict per document.
- Sample ids:
  - `BOE-A-2023-19635` (Plantas fotovoltaicas Ronda I, Ronda II y Ronda III, Cádiz/Málaga). The document's title names the plants and their evacuation infrastructure, but the only operative verdict sentence in the body is scoped to the evacuation line alone: `"formula declaración de impacto ambiental desfavorable a la realización de la línea de evacuación aéreo-subterránea 'L/220 kV Ronda - Pinar del Rey'"`. No separate favorable/unfavorable sentence for the plants themselves appears in this document (the text references a comparable project, PFV Sancho, as already having "DIA favorable" elsewhere, expediente PFot 479, but that is not this document's verdict). Treat this fixture as: line = unfavorable, plants = not adjudicated in this document.
  - `BOE-A-2023-2907` (favorable) - "Parque eólico Filabres, 153 MW, parque eólico Peregiles, 93 MW, y del parque solar fotovoltaico La Rambla, 100 MWp, ... en Tabernas, Tahal, Senés, Lucainena de las Torres, Níjar y Sorbas (Almería)". Operative sentence: `"formula declaración de impacto ambiental a la realización del proyecto ..."` with no `"desfavorable"` qualifier, followed by imposed conditions - a favorable DIA.
  - `BOE-A-2023-2580` (unfavorable) - "Plantas Solares Fotovoltaicas y sus infraestructuras de evacuación en el Nudo Jordana, en las provincias de Cádiz y Málaga". Operative sentence: `"formula declaración de impacto ambiental desfavorable a la realización del proyecto ... "Plantas Solares Fotovoltaicas y sus infraestructuras de evacuación en el Nudo Jordana ..." debido a que ... no puede descartarse que se produzcan impactos negativos significativos ..."`. Note this document names several individually-titled plants (Áncora Solar, Winche Solar, Nuza Solar I/II, Amura Solar, Trofeo Solar/Trofeo Solar II, etc.) with mixed per-plant findings in the body text even though the final operative declaration for the project as a whole is unfavorable - a good stress-test fixture for a parser that must not just keyword-match "favorable" without locating the operative sentence.

## BOJA

- OpenAPI description: `GET https://datos.juntadeandalucia.es/api/v0/boja/openapi.json` (saved to `tmp/boja_openapi.json`, gitignored - not a fixture). `info` block: `{"title": "pd-boja-ws-consultas", "description": "BOJA", "version": "0.0.1"}` - no `servers` entry; the base URL is inferred from the paths that work: `https://datos.juntadeandalucia.es/api/v0/boja/`.
- Search endpoint: `GET https://datos.juntadeandalucia.es/api/v0/boja/get/search_pagination`. Parameters, exactly as declared in the OpenAPI schema (the brief's assumed parameter names for the text query were wrong - corrected below):
  - `order_by` (required, string enum `_Enum_fields_3`: `-, id, title, number, ..., date, ..., publicUrl` etc.) - use `date` to sort by date.
  - `mode` (required, string enum `_Enum_Mode`: `DESC` / `ASC`, **uppercase** - the brief's example used lowercase `desc`, which the API rejects).
  - `size` (integer, default 0) - page size.
  - `page` (integer, default 0).
  - `general` (string, default `"-"`) - **this is the free-text query field**, not `general_search_like` as the brief assumed.
  - `general_search_like` (boolean, default false) - toggles LIKE-style matching for `general`; a plain multi-word `general` value already does a working token search without needing this flag set.
  - `summaryNoHtml` (string, default `"-"`) - an additional filter field, unused in our working query.
  - `date_from`, `date_to` (string, format `yyyy-MM-dd`, e.g. `2020-06-30` - the brief's assumed format was correct).
  - `campos` (array of `_Enum_fields_2`; repeat as `campos=a&campos=b&...` for multiple values - default `[id, organisation, summary, number, date, titleSec]` if omitted).
- Working example URL (produced `pipeline/tests/fixtures/boja_search_sample.json`, 20 results, all of them renewable-energy authorizations, out of `total_hits`: 792 matching the date window):
  ```
  https://datos.juntadeandalucia.es/api/v0/boja/get/search_pagination?order_by=date&mode=DESC&size=20&page=1&general=autorizacion+ambiental+unificada+fotovoltaica&date_from=2023-01-01&date_to=2024-12-31&campos=id&campos=organisation&campos=summary&campos=number&campos=date&campos=titleSec&campos=body&campos=bodyNoHtml&campos=publicUrl
  ```
- Response envelope: `{"hits": <int, records in this page>, "total_hits": <int, total matches>, "results": [ {...one object per record...} ]}`.
- Record fields observed in `boja_search_sample.json`: `id`, `number`, `date` (format `dd/MM/yyyy`, e.g. `"30/03/2023"` - note this differs from the `yyyy-MM-dd` input format for `date_from`/`date_to`), `titleSec` (e.g. `"3. Otras disposiciones"`, `"5. Anuncios"`), `organisation`, `summary` (HTML), `body` (HTML), `bodyNoHtml` (plain text). Requesting `campos=publicUrl` did not add a top-level `publicUrl` field - the public PDF URL instead comes back nested as `pdf: [{"publicUrl": "https://juntadeandalucia.es/eboja/...pdf", "pathPdf": ..., "id_file": ..., "hashPdf": ..., "signatureFile": ...}]` (a one-element array in every record we saw).
- Organisation name encoding note: some organisation strings contain U+00A0 (non-breaking space) between words, e.g. `"Consejería de Sostenibilidad, Medio Ambiente y\xa0Economía\xa0Azul"` - exact-string filters must normalize whitespace (e.g. collapse `\xa0` to a regular space) before comparing.
- By-id endpoint: `GET https://datos.juntadeandalucia.es/api/v0/boja/{bid}`, e.g. `https://datos.juntadeandalucia.es/api/v0/boja/disposition.2023.61.99` (id taken from a search result). Response has the same `{hits, total_hits, results}` envelope plus a `schema.fields` array documenting every possible field (33 fields total, including `pdf.publicUrl` etc. as dotted names). **The by-id response includes the full text**: `body` and `bodyNoHtml` contain the complete resolution text (in our sample, an "AAU" - autorización ambiental unificada - notice with the full "HA RESUELTO ... ANEXO ... Expediente / Promotor / Término municipal / Actuación" block), not just metadata.

### BOJA selection

- Title/summary phrases identifying relevant documents: `"autorización ambiental unificada"`, `"informe de impacto ambiental"` (also seen as `"informe vinculante sobre la autorización ambiental unificada"`), and `"información pública"` combined with a renewables word (`fotovoltaica`, `fotovoltaico`, `eólica`, `eólico`, `parque eólico`, `planta solar`).
- Organisation names publishing these authorizations, confirmed present in the 20-result sample (`boja_search_sample.json`), both from the current legislature:
  - `"Consejería de Sostenibilidad, Medio Ambiente y Economía Azul"` - the environment authority; publishes the AAU informe vinculante / DIA notices (titleSec `"5. Anuncios"` or `"3. Otras disposiciones"`).
  - `"Consejería de Economía, Hacienda y Fondos Europeos"` - the industry/energy authority; publishes grid-connection (AAP) resolutions, including denials (`"se deniega la Autorización Administrativa..."`).
  - The full historical organisation list (`GET /api/v0/boja/option-values/organisation`, 1000 values returned, not confirmed exhaustive) confirms the environment department's name has changed repeatedly across legislatures, e.g.: `"Consejería de Medio Ambiente"`, `"Consejería de Medio Ambiente y Ordenación del Territorio"`, `"Consejería de Agricultura, Pesca y Medio Ambiente"`, `"Consejería de Sostenibilidad y Medio Ambiente"`. The industry/energy side has similarly changed: `"Consejería de Industria, Energía y Minas"`, `"Consejería de Política Industrial y Energía"`, `"Consejería de Economía, Industria y Energía"`, among others. A selection filter should match on a substring/keyword basis (`"Medio Ambiente"` or `"Sostenibilidad"` for the environment side; `"Industria"` or `"Energía"` for the grid/AAP side) rather than an exact current name, given this turnover. v1 of the pipeline matches the environment side only (`ENV_DEPARTMENT_WORDS = ["sostenibilidad", "medio ambiente"]`): the energy department's grid-connection (AAP) notices are a different instrument and are excluded from the selection.

## LLM

- Provider: Groq. Live model list: `GET https://api.groq.com/openai/v1/models` with `Authorization: Bearer $IMPACTO_LLM_KEY`.
- **Deviation from the brief**: `llama-3.3-70b-versatile` is **not** in the current model list, and in fact no Llama chat/completion model is available at all. The full list of 13 models returned (2026-09-18): `allam-2-7b`, `canopylabs/orpheus-arabic-saudi`, `canopylabs/orpheus-v1-english`, `groq/compound`, `groq/compound-mini`, `meta-llama/llama-prompt-guard-2-22m`, `meta-llama/llama-prompt-guard-2-86m`, `openai/gpt-oss-120b`, `openai/gpt-oss-20b`, `openai/gpt-oss-safeguard-20b`, `qwen/qwen3.8-27b`, `whisper-large-v3`, `whisper-large-v3-turbo`. The two `llama-prompt-guard-2-*` entries are small (22M/86M parameter) safety classifiers, not usable as a general chat/completion model, so the brief's fallback ("choose the largest available Llama model") does not apply.
- **Model chosen**: `openai/gpt-oss-120b` - the largest available general-purpose chat model (120B params, 131072-token context window, supports `tools`, `json_mode`, `structured_outputs`, `reasoning`). Set `IMPACTO_LLM_MODEL=openai/gpt-oss-120b`.
- Free-tier limits for `openai/gpt-oss-120b`, confirmed two ways: (a) live response headers from a real `POST /openai/v1/chat/completions` call (`x-ratelimit-limit-requests: 1000`, `x-ratelimit-limit-tokens: 8000`), and (b) cross-checked against the published table at `console.groq.com/docs/rate-limits`, where `x-ratelimit-limit-requests` is documented as requests-per-day: **RPM 30, RPD 1000, TPM 8000, TPD 200000**.
- Operational note: an unauthenticated-looking request to `api.groq.com` without a normal `User-Agent` header gets blocked by Cloudflare with `HTTP 403` and body `error code: 1010` (bot-signature block) - Python's default `urllib` User-Agent triggers this. The pipeline's HTTP client must send a realistic `User-Agent` (a plain `curl/8.0` string was sufficient) for both the `/models` and `/chat/completions` endpoints.

### Mistral

- Second provider, selected with `--provider mistral` on `extract` and `eval`. Endpoint: `POST https://api.mistral.ai/v1/chat/completions` with `Authorization: Bearer $IMPACTO_MISTRAL_KEY`, body `{model, messages, response_format: {"type": "json_object"}, temperature: 0}`; the pipeline talks to it with `httpx` directly (`impacto.providers.mistral`), no SDK.
- Model default: `ministral-14b-latest` (`IMPACTO_MISTRAL_MODEL` overrides it).
- Free plan reality, observed 2026-09-19 with a fresh key from the console's organisation API keys page: `mistral-large-latest` answers 403 `tier_not_allowed`; `mistral-medium-latest`, `mistral-small-latest` and the `magistral-*` models answer 429 with `x-ratelimit-limit-req-minute: 0`, so they are not enabled on this plan; `ministral-3b`, `ministral-8b`, `ministral-14b` and `voxtral-small` accept requests. The 14B model is the strongest available and is the default. No daily cap was hit in about 60 requests over four documents (about 20 minutes), only per-second pacing.
- Quality on the four labelled documents with `ministral-14b-latest` and prompt v3 plus the operative-sentence rule: developer, doc_type, municipalities, technology and verdict 100 percent; mw_nominal 50 percent, both misses on three-park hybrid resolutions where the model swaps nominal and peak power for wind parks (BOE-A-2023-2907 got 169 of 326; BOE-A-2023-2580 got 740 of 801). Recorded in `pipeline/evaluation/last_run.json`.
- Limits: Mistral does not publish the free Experiment plan's rate and monthly token limits in a machine-readable place and the API does not return them as response headers; they are only visible in the Mistral console (La Plateforme, "Limits" page for the workspace). The provider therefore treats a `429` whose body names a daily or monthly cap ("per day", "tpd", "per month", "monthly") as terminal (`QuotaExhausted`) and retries any other `429` or `5xx` with the same 10 s doubling backoff as Groq, up to 6 attempts.

## Extraction observations

- **Operative sentence rule (2026-09-19)**: the ministry's favourable DIA form is "formula declaración de impacto ambiental a la realización del proyecto ... en la que se establecen las condiciones", with no adjective at all, and small models return `no_aplica` for it. `impacto.extract.operative` now reads the operative sentence by rule (favourable and unfavourable DIA, line-only refusal with conditions, AAU granted or denied, last match wins) and overrides the model's doc_type and verdict when it matches; the sentence is stored as the evidence. Verified on the three BOE fixtures and the BOJA Los Barrios denial.
- **Per-plant lists (prompt v3)**: multi-plant resolutions state "93 MWn cada una" and never print a total; prompt v3 asks for one number per plant for mw_peak, mw_nominal, hectares and turbines, and the sanitiser sums lists or dicts of numbers and joins lists of strings (developer, project_name). Conditions without text and municipalities without a name are dropped.

- **Daily token quota exhausted during Task 9 development**: repeated `uv run python -m impacto extract --limit 3` runs against the dev database (needed to reproduce and fix several real response-shape mismatches - see `impacto.extract.run._sanitize`) used up the account's free-tier daily budget for `openai/gpt-oss-120b`. The final real run left 1 of 3 documents extracted (`status = 'ok'`); the other two failed with `HTTP 429`, `type: "tokens"`, `code: "rate_limit_exceeded"`, message `Rate limit reached ... on tokens per day (TPD): Limit 200000, Used ~198500-199300, Requested ~2500-3100. Please try again in Xm`, with the quoted wait ranging 8-17 minutes at the time each request was made. This confirms the TPD 200000 figure recorded above is a hard daily cap (not just TPM 8000 per-minute), and that a handful of full 3-document extraction runs during development is enough to exhaust it for the day. Both failures were recorded as `status = 'failed'` rows with `attempts = 1` (2 retries left before the 3-attempt cap) rather than crashing the stage, and `GroqProvider`'s own 6-attempt exponential backoff (10s/20s/40s/80s/160s) ran to exhaustion correctly before the underlying `RateLimitError` was re-raised and caught by `run_extract` - the backoff logic works as designed, it simply cannot out-wait a multi-minute daily-quota reset.
- **Document 5 verdict looks wrong**: BOJA record `disposition.2023.169.46` (title: "...se da publicidad al informe vinculante con el que **se deniega** la autorización ambiental unificada para el proyecto que se cita, en el término municipal de Los Barrios (Cádiz)") extracted with `status = 'ok'`, `confidence = 0.88`, `project_name = "PSFV Pinar del Rey II"`, `municipalities = [{"name": "Los Barrios", "province": "Cádiz"}]`, but `verdict = "no_aplica"`. The document's own title states the AAU is denied, which per the v1 prompt's rule should be `verdict = "desfavorable"` (the operative part refuses the project itself, not an accessory part). This looks like a model miss rather than a prompt-wording issue covered by this task's ruling 5 (that ruling is about the *favorable_condicionada vs desfavorable* distinction for partial refusals, and this document doesn't have that ambiguity - it is a wholesale AAU denial). Per this task's scope, prompt tuning beyond ruling 5 is deferred to Task 13's evaluation set; recorded here for that pass.
- The Ronda document (BOE-A-2023-19635, `raw_documents.id = 3`) never reached `status = 'ok'` during Task 9: each real run surfaced a different non-conforming response shape from the model (null on required enums, `technology` returned as a list, `evidence` values returned as lists or null, an out-of-schema `conditions[].category` value `"poblacion"`, an out-of-schema `doc_type` value `"declaracion_impacto"`), each fixed in `impacto.extract.run._sanitize` with a unit test reproducing the exact real failure, and the final attempt was blocked by the daily quota above before a clean run could be confirmed. Its verdict is therefore unknown as of this task; re-run `uv run python -m impacto extract --limit 3` once the daily quota resets to get it and check it against this same "Ronda desfavorable" observation point from the task brief.

## Reference layers

- Municipality boundaries: **obtained**. Download URL: `https://www.juntadeandalucia.es/institutodeestadisticaycartografia/dega/sites/default/files/datos/094-dera-13-limites-administrativos.zip` (76589276 bytes; linked from the DERA download page `https://www.juntadeandalucia.es/institutodeestadisticaycartografia/dega/datos-espaciales-de-referencia-de-andalucia-dera/descarga-de-informacion`, theme 13 "Límites administrativos", shapefile format). The archive contains 190 files across many administrative layers; the municipal-boundary layer is `13_01_TerminoMunicipal.shp` (+ .dbf/.shx/.prj/.cpg). Inspected directly (pyshp, since `ogrinfo`/geopandas were unusable on this machine - see note below): format = Shapefile, CRS = `ETRS_1989_UTM_Zone_30N` per the `.prj` WKT (same projection parameters as EPSG:25830, confirmed independently via a GeoTIFF's `GeoKeyDirectoryTag` for the sensitivity-zoning layer below, which reports `ProjectedCSTypeGeoKey = 25830` for the identical WKT string), feature count = 785 (one row per Andalucía municipality), fields = `id_dera` (numeric id), `cod_mun` (5-character INE municipality code, e.g. `"04012"`), `nombre` (municipality name), `provincia` (province name).
- Natura 2000: **not obtained**. Landing page: `https://www.miteco.gob.es/es/cartografia-y-sig/ide/descargas/biodiversidad/rn2000.html`, which links to `https://gis.miteco.gob.es/descargas/app/DescargaFichero?f=rn2000.zip`. That URL is a JavaScript single-page app gated by an ALTCHA "I'm not a robot" proof-of-work captcha; `curl` (with or without `-L`) only ever receives the HTML shell of the app, never the file. An older cached URL for the same dataset, `https://www.mapama.gob.es/app/descargas/descargafichero.aspx?f=rn2000.zip`, 301-redirects to a dead path on `www.mapa.gob.es` (404) and is no longer valid. Solving the captcha in a real browser (Chrome via automation) succeeded (checkbox showed "Verified") and clicking "Descargar fichero" appeared to trigger a server-side download, but no resulting file could be confirmed retrievable into this project's `tmp/reference/` directory or the machine's default Downloads folder within this session - so per the task's ruling on form-gated downloads, this is recorded as not obtained rather than guessed. Format/CRS/site-code field are therefore unknown and must not be assumed. A documented WMS endpoint exists (`https://wms.mapama.gob.es/sig/Biodiversidad/RedNatura/wms.aspx`) that a later task could use for point/polygon queries without the bulk-download captcha, or a human could complete the captcha once and drop the resulting file into `tmp/reference/` for a follow-up task to inspect.
- Sensitivity zoning ("zonificación ambiental para energías renovables"): **obtained**, but as raster, not vector. Landing page: `https://www.miteco.gob.es/en/calidad-y-evaluacion-ambiental/temas/evaluacion-ambiental/zonificacion_ambiental_energias_renovables.html`. Downloaded the "classified" (5-class) files for both technologies:
  - Wind: `https://www.miteco.gob.es/content/dam/miteco/es/calidad-y-evaluacion-ambiental/temas/evaluacion-ambiental-de-planes-programas-y-proyectos/Zonificacion_EOL_clasificada_2023.zip` (36095940 bytes)
  - Photovoltaic: `https://www.miteco.gob.es/content/dam/miteco/es/calidad-y-evaluacion-ambiental/temas/evaluacion-ambiental-de-planes-programas-y-proyectos/Zonificacion_FTV_clasificada_2023.zip` (42697803 bytes)
  Each zip contains two GeoTIFFs (`Clas_ISA_<eol|ftv>_c.tiff`, smaller, and `Clas_ISA_<eol|ftv>_pb.tiff`, larger - the included readme `leeme_valores ISA.pdf` says to use the classified option, i.e. `_pb`, for GIS use) plus that readme PDF. Inspected `Clas_ISA_eol_pb.tiff` directly with `tifffile`/`imagecodecs` (pure-Python, no GDAL - see note below): single-band `uint16` raster, 38676 x 45644 pixels, 25m x 25m pixel size, covering peninsular Spain (not just Andalucía). CRS confirmed as **EPSG:25830** (ETRS89 / UTM zone 30N) via the embedded `GeoKeyDirectoryTag` (`ProjectedCSTypeGeoKey = 25830`). "Class field" is the pixel value itself (band 1); observed values and their meaning, per `leeme_valores ISA.pdf`: `0` = Máxima (no recomendado), `1` = Muy alta, `2` = Alta, `3` = Moderada, `4` = Baja, `65535` = nodata (uint16 max, used outside Spanish territory). The photovoltaic file was downloaded but not separately value-inspected; it ships the same readme and is expected to share this schema.

### Note on tooling for this task

`ogrinfo` was not installed, and the ruling's fallback command (`uv run --with geopandas --with pyogrio ...`) failed on this machine with `ImportError: DLL load failed while importing _cyutility / nattype: Una directiva de Control de aplicaciones bloqueó este archivo` (a Windows Application Control policy blocking pandas' compiled Cython extensions) - this reproduced identically under both the system Python (3.14) and a uv-provisioned Python 3.12, so it is a machine policy, not a version mismatch. Vector inspection was done instead with the pure-Python `pyshp` package (no compiled extensions), and raster inspection with `tifffile` + `imagecodecs`. Both avoided the blocked DLL path and produced results cross-checked against each dataset's own metadata (the `.prj` WKT for the shapefile; the `leeme_valores ISA.pdf` readme and embedded GeoTIFF tags for the raster).

## Task 10 load results (reference layers)

Natura 2000 status is upgraded from Task 1's "not obtained" (the MITECO `rn2000.zip` download stayed captcha-gated when re-tried in this task, confirmed by re-checking `tmp/reference/miteco_natura2000.zip`: it is 28283 bytes of HTML shell, not a zip - `unzip -l` fails with "End-of-central-directory signature not found" and the first bytes are `<!DOCTYPE html> ...`, i.e. the ALTCHA app page, not the file). Per ruling 3(a), an Andalusia-only Natura 2000 source was found on the DERA/REDIAM side instead, and it worked with a plain `curl` (no captcha, no browser automation):

- Dataset page: `https://www.juntadeandalucia.es/datosabiertos/portal/dataset/limites-de-la-red-natura-2000-lic-zec-y-zepa-en-andalucia-informacion-actualizada` (Junta de Andalucía open-data portal). Its featured resource (id `bbca0afd-2a0b-4964-a7b5-0e0a43ac1704`) is a direct, unauthenticated download: `https://www.juntadeandalucia.es/medioambiente/servtorrent/informacionambiental/07_PATRIMONIO_NATURAL/01_RENPA/10_RED_NATURA_2000/RedNatura2000.tar.gz` (`curl -I` returns `200 OK`, `Content-Type: application/x-gzip`, `Content-Length: 7368208`; `Last-Modified: Wed, 18 May 2016`, so this is the REDIAM 2016 vintage of the layer, not the "noviembre 2025" update mentioned by the newer GeoNetwork record found in the same search - a more recent Andalusia-specific download was not found via this portal). Downloaded to `tmp/reference/rediam_red_natura_2000.tar.gz` and extracted to `tmp/reference/natura2000_extracted/`. Contains `RedNatura2000/InfGeografica/InfVectorial/Shapes/RedNatura2000_Andalucia.shp` (+ .dbf/.shx/.prj/.sbn/.sbx, no .cpg), CRS `ETRS_1989_UTM_Zone_30N` (EPSG:25830, same WKT as the municipalities shapefile), 252 features. Fields: `CODIGOEURO` (site code, e.g. `"ES0000025"`), `NOMBRE` (name, e.g. `"MARISMAS DEL ODIEL"`), `FIGURA` (type: `ZEC`/`ZEPA`/`LIC`, counts 141/63/48), `FECHADECLA` (declaration date), `SUPERFICIE` (area ha). No `.cpg` file ships with this shapefile; its `.dbf` text is Windows-1252 (`Ñ` in `"SIERRA DE CARDEÑA Y MONTORO"` decodes correctly as `cp1252`/fails as strict `utf-8`), unlike the DERA municipalities shapefile which does ship a `.cpg` of `UTF-8`. 55 of the 252 `CODIGOEURO` values repeat (a site can carry LIC, ZEC and ZEPA designations under the same code) - `load_protected_areas`'s `ON CONFLICT (site_code) DO NOTHING` (unchanged from the brief) keeps the first-seen geometry per code and silently drops the rest, so the function's return value (252, matching "features read", per its documented contract) is larger than the actual row count in `protected_areas` (197). This is a pre-existing property of the brief's function, not new to this task; recorded here because it is easy to miss.

### Real load commands and row counts (dev database, `uv run python -m impacto migrate` applied first)

```
cd pipeline
uv run python -m impacto reference municipalities ../tmp/reference/dera_extracted/13_01_TerminoMunicipal.shp --code cod_mun --name nombre --province provincia
# -> loaded 785 feature(s) into municipalities (matches Task 1's inspected count; 0 rows outside the 8 Andalusian provinces)

uv run python -m impacto reference protected-areas ../tmp/reference/natura2000_extracted/RedNatura2000/InfGeografica/InfVectorial/Shapes/RedNatura2000_Andalucia.shp --code CODIGOEURO --name NOMBRE --type FIGURA
# -> loaded 252 feature(s) into protected-areas (252 read, 197 distinct site_code rows actually stored - see dedup note above)

uv run python -m impacto reference sensitivity ../tmp/reference/eol_extracted/Clas_ISA_eol_pb.tiff --technology eol
# -> sensitivity[eol]: 19985 polygon(s) in 7.6s (factor=10)  [maxima 13473, muy_alta 2202, alta 4310]

uv run python -m impacto reference sensitivity ../tmp/reference/ftv_extracted/Clas_ISA_ftv_pb.tiff --technology ftv
# -> sensitivity[ftv]: 27179 polygon(s) in 8.4s (factor=10)  [maxima 17832, muy_alta 2012, alta 7335]
```

Both sensitivity loads stayed well inside the ruling's thresholds (< 300000 rows, < 15 minutes) at the default `factor=10` (250m output pixel), so no fallback to `factor=20` was needed. All four commands were re-run a second time and produced identical row counts (municipalities 785, protected-areas 252/197, sensitivity eol 19985, sensitivity ftv 27179), confirming the loaders are repeatable.

### Deviation: rasterio/pyogrio import but cannot load data on this machine

The task instructed adding `rasterio` and `pyogrio` to `pyproject.toml` on the strength of the controller having verified `import rasterio` etc. work under `uv run --python 3.12` on this machine. Both packages were added and `uv sync` installs them cleanly, but at runtime, on this machine, right now: `import rasterio` fails with `ImportError: DLL load failed while importing _base: Una directiva de Control de aplicaciones bloqueó este archivo`, and `import pyogrio` fails the same way (`DLL load failed while importing _ogr`, then `GDAL DLL could not be found`) - both point at the bundled `gdal-*.dll` in `rasterio.libs`/`pyogrio.libs`, blocked by the same Windows Application Control policy Task 1 already hit and documented above (confirmed: the DLL carries no Mark-of-the-Web/Zone.Identifier stream, so this is a policy/hash block, not a downloaded-file quarantine that a retry or `Unblock-File` would clear). `geopandas.read_file(...)` therefore also fails (`neither pyogrio nor fiona ... imports correctly`; `fiona` is not installed). By contrast `shapely`, `numpy` and `pyproj` (which also bundles a compiled `proj.dll`) import and work fine, matching Task 1's finding that some compiled-extension packages are blocked and others are not.

Given this, `pipeline/impacto/reference/load.py` does not actually use `rasterio`/`pyogrio`/`geopandas.read_file` for I/O (both packages stay in `pyproject.toml` per the task's instruction, and would be used automatically if a future environment allows it, but nothing currently imports them). Instead, following the same pure-Python approach Task 1 used to inspect these files:

- Vector reading (`.shp` and `.geojson`) goes through `pyshp` (`shapefile.Reader`, attributes to dict, `shape.__geo_interface__` fed straight into `shapely.geometry.shape`) plus a small explicit `.prj`-WKT-to-EPSG lookup (currently one entry: `ETRS_1989_UTM_Zone_30N` -> 25830, covering both shapefiles used here; an unrecognized `.prj` raises rather than guessing, per the "don't guess CRS/APIs" instruction). `.cpg`-declared encoding is used when present, `cp1252` otherwise (see the Natura 2000 encoding note above).
- Raster reading goes through `tifffile`, reading the `ModelPixelScaleTag`/`ModelTiepointTag` GeoTIFF tags directly for the affine transform, then decoding only the tiles that intersect the municipalities' bounding box (`page.decode()` on each tile's raw bytes, seeked and read individually via `TiffFile.filehandle`) - this is the closest pure-Python equivalent of rasterio's windowed read, and is fast in practice (15604 tiles / ~511MB decoded window over roughly the full Andalusia bbox in 0.45s during development benchmarking). The window is decimated by `factor` via numpy striding (`array[::factor, ::factor]`, nearest-neighbour) after decoding, rather than during decode as `rasterio`'s `out_shape` would do - given the window itself (not the full 38676x45644 raster) is what gets decoded, this stayed under 600MB in practice and was not a memory problem, but it is a real deviation from "the full-resolution window is never loaded into memory" and is recorded here rather than glossed over.
- Polygonisation (no `rasterio.features.shapes` available) is done by run-length-merging each decimated row into horizontal spans per class, then `shapely.union_all` over all of a class's span-rectangles, which lets GEOS dissolve touching/overlapping spans (within and across rows) into the same per-connected-region polygons `rasterio.features.shapes` would produce (4-connectivity, since only edge-touching rectangles merge) - then each disconnected piece of the unioned (multi)polygon becomes its own row, matching the granularity the brief's row-count ruling (300000/15 min) implies. This was benchmarked against the real EOL raster at both factor 10 and 20 before the real run (see counts above) to confirm it would not blow past those limits.

This is a substantially different implementation path than the brief anticipated, but the loader signatures, CLI shape, migration, and observable behaviour (row counts, klass values, repeatability, CRS/geometry-type of stored rows) all match what the task specified.
